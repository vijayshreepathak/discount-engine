# Optional — only required for Natural Language Rule parsing (Anthropic Claude)
# Copy to .env and add your key:  cp .env.example .env
#
# VITE_ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxx
#
# Optional model override (defaults to claude-sonnet-4-6):
# VITE_ANTHROPIC_MODEL=claude-sonnet-4-6

# No environment variables are required for CSV upload, PDF upload,
# discount calculation, or export unless you enable AI rule creation.
